Merge the three QA reports into one comprehensive report by running:

```bash
scripts/merge-qa-reports.sh
```

If any of the three JSON reports are missing from `reports/data/`, let the user know which ones are needed before proceeding.
