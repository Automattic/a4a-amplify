{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "mkdir -p ${CLAUDE_PLUGIN_ROOT}/reports/data ${CLAUDE_PLUGIN_ROOT}/reports/screenshots"
          }
        ]
      }
    ]
  }
}